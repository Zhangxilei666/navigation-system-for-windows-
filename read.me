# 上位机导航系统（初始版本）

这是我最开始实现的上位机导航系统原型，核心代码在 `try.py`。

当前阶段定位为“功能雏形验证”，主要目标是先跑通界面与传感器数据接入，再逐步完善与下位机联调。

## 当前进度

- 已完成：
  - 可启动图形界面（PyQt5）
  - 串口列表刷新与端口选择
  - G60 模块连接测试通过
  - H30 模块连接测试通过
  - 基础数据显示、地图显示、姿态显示框架已搭建

- 未完成：
  - 暂未与下位机完成通信联调
  - 下位机控制指令收发与闭环控制尚未实测
  - 系统整体稳定性与异常工况测试尚未完成

## 功能概览（基于当前代码）

- 双设备串口接入：G60（定位）+ H30（姿态）
- 数据可视化：
  - 地图轨迹显示（含卫星地图组件）
  - 姿态显示（含 3D 姿态相关界面）
  - 实时信息面板/状态栏
- 数据记录：支持记录并导出 CSV
- 基础配置：地图刷新、串口参数等

## 运行环境

建议使用 Python 3.9+（Windows 环境已验证）。

### 主要依赖

`try.py` 中使用到的主要第三方库：

- `pyserial`
- `numpy`
- `folium`
- `PyQt5`
- `PyQtWebEngine`
- `pyqtgraph`
- `geographiclib`

## 安装依赖

在项目目录执行：

```bash
pip install pyserial numpy folium pyqt5 pyqtwebengine pyqtgraph geographiclib
```

## 启动方式

在项目目录执行：

```bash
python try.py
```

## 使用说明（当前阶段）

1. 打开程序后，先点击刷新串口列表。
2. 分别选择 G60 与 H30 对应端口及波特率。
3. 点击连接按钮，观察状态灯/状态栏是否显示连接成功。
4. 当前主要验证连接与数据显示链路；与下位机通信功能暂未联调，请勿用于正式控制任务。

## 已知说明

- 代码中存在部分注释/字符串编码显示异常（历史文件编码问题），不影响本阶段“连接验证”使用。
- 地图在线瓦片依赖网络，网络不稳定时可能出现加载慢或失败。

## 后续计划

- 完成与下位机通信协议联调（收发、超时、重连机制）
- 增加通信日志与异常告警
- 完善参数配置持久化
- 增加联调测试用例与回放数据

## 免责声明

当前为早期开发版本，仅用于学习与开发调试，不建议直接用于实地无人值守作业。
